# Can we have a set with 18 (int) and '18' (str) as a value in it?
# lets check
# yes we can have because one is int and other is str and in sets we can add string and int both
s = {18 , '18'};
print(s);
